services:
  deployment-dashboard:
    build: .
    container_name: deployment-app
    ports:
      - "1234:1234"
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
